import styled from "styled-components";

export const StyledInput = styled.input`
  width: calc(100% - 20px);
  border: var(--border-gray);
  padding: 20px;
  font-size: 18px;
  border-radius: 5px;
`;
